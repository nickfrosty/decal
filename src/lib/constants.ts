/**
 * Storage key values used to read and write specific data to local storage (aka `AsyncStorage`)
 */
export const STORAGE_KEY = {
  connection: "KEY_NETWORK_CONNECTION",
  userWallet: "KEY_SELECTED_USER_WALLET",
};
